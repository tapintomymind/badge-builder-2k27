/**
 * Placeholder shell. Intentionally rule-free.
 *
 * The real UI is built in a later milestone and will replace this file wholesale.
 * Nothing in src/ui/ may compute a rule — every number comes from src/engine/.
 */
export default function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: "42rem" }}>
      <h1>Badge Builder 2K27</h1>
      <p>Skeleton scaffolded. No engine, no dataset, no UI yet.</p>
    </main>
  );
}
